function mostrarTablaMult(numero){
    for (let i = 1; i<=10; i++){
        let resultado = numero * i;
        console.log(numero + " X " + i + " = " + resultado);
    }
}
let numero = 7;
mostrarTablaMult(numero);